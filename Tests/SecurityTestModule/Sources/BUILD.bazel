load("//bazel:swift_rules.bzl", "umbra_swift_library")

umbra_swift_library(
    name = "SecurityTestModuleSources",
    srcs = glob(
        [
            "*.swift",
            "**/*.swift",
        ],
        allow_empty = False,
        exclude = [
            "**/*.generated.swift",
        ],
        exclude_directories = 1,
    ),
    testonly = 1,  # Mark as test-only since it's in the Tests directory
    visibility = ["//visibility:public"],  # Make it visible to other test targets
    deps = [
        # Dependencies will go here, for example:
        # "//Sources/CoreDTOs",
        # "//Sources/UmbraLogging",
        # "@com_github_apple_swift_log//:Logging",
    ],
)
