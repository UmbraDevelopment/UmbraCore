
import Foundation
import UmbraErrors
import UmbraErrorsCore

/// Root namespace for all UmbraCore error domains
/// Acts as a container for domain-specific error types
public enum UmbraErrors {
  // This empty enum serves as a namespace container only
}
